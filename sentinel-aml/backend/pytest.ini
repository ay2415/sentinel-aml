[pytest]
testpaths = tests
addopts = -q -p no:cacheprovider
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    postgres: requires TEST_DATABASE_URL pointing at PostgreSQL with pgvector
